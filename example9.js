var name=prompt("Input the user name:");
var password=prompt("Input the password:");
if(name=="Sophanon"){
    if(password=="123"){
        document.write("Welcome.....");
    }
    else{
        document.write("Wrong password....");
    }
}
else{
    document.write("Wrong username....");
}